{
  "name": "turbo-changeset",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@coconnorco/turbo-changeset-eslint-config": "*",
    "@coconnorco/turbo-changeset-typescript-config": "*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
